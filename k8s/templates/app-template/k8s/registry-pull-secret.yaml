# ExternalSecret: pulls Forgejo registry credentials from Vault and creates
# a docker-registry secret in this namespace. Requires:
#   - External Secrets Operator running on the workload cluster
#   - ClusterSecretStore 'vault-backend' pointing to the platform Vault
#   - Vault path secret/forgejo/registry-pull-token containing:
#       username: <deploy-token-username>
#       token:    <deploy-token-value>
#
# Created by: register-workload-cluster.sh
# Vault path populated by: register-workload-cluster.sh (Forgejo API token)
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: registry-pull-secret
  namespace: devhub-APP_NAME
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: registry-pull-secret
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {
            "auths": {
              "git.DOMAIN": {
                "username": "{{ .username }}",
                "password": "{{ .token }}",
                "auth": "{{ printf "%s:%s" .username .token | b64enc }}"
              }
            }
          }
  data:
    - secretKey: username
      remoteRef:
        key: forgejo/registry-pull-token
        property: username
    - secretKey: token
      remoteRef:
        key: forgejo/registry-pull-token
        property: token
