apiVersion: v1
kind: Service
metadata:
  name: APP_NAME
  namespace: devhub-APP_NAME
  labels:
    app: APP_NAME
spec:
  type: ClusterIP
  selector:
    app: APP_NAME
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
