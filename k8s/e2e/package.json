{
  "name": "devhub-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end validation of a deployed devhub platform: every service reachable, SSO working, and each component holding real data.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
