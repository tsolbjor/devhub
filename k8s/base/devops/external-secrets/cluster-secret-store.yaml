# ClusterSecretStore for Vault
# Apply after both Vault and External Secrets Operator are installed
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: vault-backend
spec:
  provider:
    vault:
      server: "http://vault.vault.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "external-secrets"
          serviceAccountRef:
            name: "external-secrets"
            namespace: "external-secrets"
---
# Example ExternalSecret
# This shows how to sync a secret from Vault to Kubernetes
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: example-secret
#   namespace: devhub
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: example-secret
#     creationPolicy: Owner
#   data:
#     - secretKey: password
#       remoteRef:
#         key: secret/data/myapp
#         property: password
