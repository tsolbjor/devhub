# Vault local overlay values
# Merged with base/devops/vault/values.yaml
# Uses ${DOMAIN} from config.yaml

server:
  # Single replica for local development
  ha:
    enabled: true
    replicas: 1

# Single injector replica for local
injector:
  replicas: 1
