apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DevOps platform local overlay
# Gateway API: listeners use the local CA secret; routes in httproutes.yaml

resources:
  - gateway.yaml
  - httproutes.yaml

labels:
  - pairs:
      environment: local
      component: devops
