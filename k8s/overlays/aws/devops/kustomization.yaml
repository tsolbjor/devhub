apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DevOps platform AWS overlay
# Ingress configurations with cert-manager TLS

resources:
  - ingress.yaml

labels:
  - pairs:
      environment: production
      component: devops
