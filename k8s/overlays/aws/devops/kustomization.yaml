apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DevOps platform AWS overlay
# Gateway API: listeners + TLS in gateway.yaml, routes in httproutes.yaml

resources:
  - gateway.yaml
  - httproutes.yaml

labels:
  - pairs:
      environment: production
      component: devops
